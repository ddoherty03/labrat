#! /usr/bin/env ruby

require_relative '../lib/labrat'

ops = Labrat::ArgParser.new.parse(ARGV)
if ops.msg
  warn ops.msg
  exit(1)
end

non_ops = ARGV.grep_v(/\A--?/)
if non_ops.empty?
# Read from STDIN
else
  text = non_ops.join(' ')
end

lab = Labrat::Label.new(text, ops)
lab.generate
